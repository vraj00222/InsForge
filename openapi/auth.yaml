openapi: 3.0.3
info:
  title: Insforge Authentication API
  version: 2.0.0
  description: Authentication endpoints with separated auth and profile tables

paths:
  /api/auth/public-config:
    get:
      summary: Get public authentication configuration
      description: Get all public authentication configuration including OAuth providers and email auth settings (public endpoint)
      tags:
        - Client
      responses:
        '200':
          description: Public authentication configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  oAuthProviders:
                    type: array
                    items:
                      type: string
                      enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
                  customOAuthProviders:
                    type: array
                    items:
                      type: string
                  requireEmailVerification:
                    type: boolean
                  passwordMinLength:
                    type: integer
                    minimum: 4
                    maximum: 128
                  requireNumber:
                    type: boolean
                  requireLowercase:
                    type: boolean
                  requireUppercase:
                    type: boolean
                  requireSpecialChar:
                    type: boolean
                  verifyEmailMethod:
                    type: string
                    enum: [code, link]
                    description: Method for email verification (code = 6-digit OTP, link = magic link)
                  resetPasswordMethod:
                    type: string
                    enum: [code, link]
                    description: Method for password reset (code = 6-digit OTP + exchange flow, link = magic link)
                  disableSignup:
                    type: boolean
                    description: When true, new user sign-ups are disabled. Existing users can still sign in.

  /api/auth/profiles/current:
    patch:
      summary: Update current user's profile
      description: Update the profile of the currently authenticated user
      tags:
        - Client
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - profile
              properties:
                profile:
                  type: object
                  additionalProperties: true
                  description: Profile data (name, avatar_url, and any custom fields)
                  properties:
                    name:
                      type: string
                    avatar_url:
                      type: string
                      format: uri
      responses:
        '200':
          description: Profile updated successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProfileResponse'
        '400':
          description: Invalid request
        '401':
          description: Unauthorized - authentication required

  /api/auth/profiles/{userId}:
    get:
      summary: Get user profile by ID
      description: Get public profile information for a user by their ID (public endpoint)
      tags:
        - Client
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: User ID
      responses:
        '200':
          description: User profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProfileResponse'
        '400':
          description: Invalid user ID format
        '404':
          description: User not found

  /api/auth/config:
    get:
      summary: Get authentication configuration
      description: Get current authentication settings including all configuration options (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Authentication configuration
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  requireEmailVerification:
                    type: boolean
                  passwordMinLength:
                    type: integer
                    minimum: 4
                    maximum: 128
                  requireNumber:
                    type: boolean
                  requireLowercase:
                    type: boolean
                  requireUppercase:
                    type: boolean
                  requireSpecialChar:
                    type: boolean
                  verifyEmailMethod:
                    type: string
                    enum: [code, link]
                    description: Method for email verification (code = 6-digit OTP, link = magic link)
                  resetPasswordMethod:
                    type: string
                    enum: [code, link]
                    description: Method for password reset (code = 6-digit OTP + exchange flow, link = magic link)
                  allowedRedirectUrls:
                    type: array
                    items:
                      type: string
                    description: List of allowed URLs for authentication redirects. If empty, all redirects are allowed for smoother development UX. This is not recommended in production.
                  disableSignup:
                    type: boolean
                    description: When true, public sign-up endpoints (POST /api/auth/users and first-time OAuth) are rejected with 403 AUTH_SIGNUP_DISABLED. Admin-authenticated user creation is unaffected.
                  createdAt:
                    type: string
                    format: date-time
                  updatedAt:
                    type: string
                    format: date-time
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

    put:
      summary: Update authentication configuration
      description: Update authentication settings (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                requireEmailVerification:
                  type: boolean
                  description: Enabling this requires custom SMTP or a managed email provider.
                passwordMinLength:
                  type: integer
                  minimum: 4
                  maximum: 128
                requireNumber:
                  type: boolean
                requireLowercase:
                  type: boolean
                requireUppercase:
                  type: boolean
                requireSpecialChar:
                  type: boolean
                verifyEmailMethod:
                  type: string
                  enum: [code, link]
                  description: Method for email verification (code = 6-digit OTP, link = magic link)
                resetPasswordMethod:
                  type: string
                  enum: [code, link]
                  description: Method for password reset (code = 6-digit OTP + exchange flow, link = magic link)
                allowedRedirectUrls:
                  type: array
                  items:
                    type: string
                  description: List of allowed URLs for authentication redirects. If empty, all redirects are allowed for smoother development UX. This is not recommended in production.
                disableSignup:
                  type: boolean
                  description: When true, public sign-up endpoints (POST /api/auth/users and first-time OAuth) are rejected with 403 AUTH_SIGNUP_DISABLED. Admin-authenticated user creation is unaffected.
      responses:
        '200':
          description: Configuration updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: string
                    format: uuid
                  requireEmailVerification:
                    type: boolean
                  passwordMinLength:
                    type: integer
                    minimum: 4
                    maximum: 128
                  requireNumber:
                    type: boolean
                  requireLowercase:
                    type: boolean
                  requireUppercase:
                    type: boolean
                  requireSpecialChar:
                    type: boolean
                  verifyEmailMethod:
                    type: string
                    enum: [code, link]
                  resetPasswordMethod:
                    type: string
                    enum: [code, link]
                  allowedRedirectUrls:
                    type: array
                    items:
                      type: string
                    description: List of allowed URLs for authentication redirects. If empty, all redirects are allowed for smoother development UX. This is not recommended in production.
                  disableSignup:
                    type: boolean
                    description: When true, public sign-up endpoints (POST /api/auth/users and first-time OAuth) are rejected with 403 AUTH_SIGNUP_DISABLED. Admin-authenticated user creation is unaffected.
                  createdAt:
                    type: string
                    format: date-time
                  updatedAt:
                    type: string
                    format: date-time
        '400':
          description: Invalid request, or no email provider is configured when enabling email verification
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

  /api/auth/users:
    post:
      summary: Register new user
      description: |
        Creates a new user account. When email verification is required, a delivery failure is returned as an error after the account is created. Retry delivery with POST /api/auth/email/send-verification instead of registering the same email again.
      tags:
          - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: |
            Client type determines how refresh tokens are returned:
            - web: Refresh token stored in httpOnly cookie, csrfToken returned in response
            - mobile/desktop/server: refreshToken returned directly in response body
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - password
              properties:
                email:
                  type: string
                  format: email
                  example: user@example.com
                password:
                  type: string
                  description: Password meeting configured requirements (check /api/auth/email/config for current requirements)
                  example: securepassword123
                name:
                  type: string
                  example: John Doe
                redirectTo:
                  type: string
                  format: uri
                  description: >-
                    Used for link-based email verification. The email link always
                    opens an InsForge backend endpoint first; after the token is
                    verified, InsForge redirects the browser to this URL. This URL
                    must be included in allowedRedirectUrls. Recommended: use your
                    app's sign-in page.
      responses:
        '200':
          description: User created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserResponse'
                  accessToken:
                    type: string
                    nullable: true
                    description: JWT authentication token (null if email verification required)
                  csrfToken:
                    type: string
                    nullable: true
                    description: CSRF token for use with refresh endpoint (web clients only, null if email verification required)
                  refreshToken:
                    type: string
                    nullable: true
                    description: Refresh token for mobile/desktop/server clients (null for web clients or if email verification required)
                  requireEmailVerification:
                    type: boolean
                    description: Whether email verification is required before login
        '400':
          description: Invalid request
        '403':
          description: Signups disabled — project has disabled new user registration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: AUTH_SIGNUP_DISABLED
                message: User signups are disabled for this project.
                statusCode: 403
        '409':
          description: User already exists
        '429':
          description: User created, but verification email delivery was rate limited
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: AUTH_VERIFICATION_EMAIL_DELIVERY_FAILED
                message: The user account was created, but the verification email could not be sent.
                statusCode: 429
                nextActions: The user account already exists. Retry delivery with POST /api/auth/email/send-verification instead of registering again.
        '500':
          description: User created, but the verification email could not be sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                error: AUTH_VERIFICATION_EMAIL_DELIVERY_FAILED
                message: The user account was created, but the verification email could not be sent.
                statusCode: 500
                nextActions: The user account already exists. Retry delivery with POST /api/auth/email/send-verification instead of registering again.

    get:
      summary: List all users (admin only)
      description: Returns paginated list of users
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: offset
          in: query
          schema:
            type: string
            default: '0'
          description: Number of records to skip
        - name: limit
          in: query
          schema:
            type: string
            default: '10'
          description: Maximum number of records to return
        - name: search
          in: query
          schema:
            type: string
          description: Search by email or name
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/UserResponse'
                  pagination:
                    type: object
                    properties:
                      offset:
                        type: integer
                      limit:
                        type: integer
                      total:
                        type: integer
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

    delete:
      summary: Delete users (admin only)
      description: Delete multiple users by their IDs
      tags:
        - Admin
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                userIds:
                  type: array
                  items:
                    type: string
              required:
                - userIds
      responses:
        '200':
          description: Users deleted successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                  deletedCount:
                    type: integer
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

  /api/auth/users/{userId}:
    get:
      summary: Get specific user
      description: Get user details by ID (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: User ID
      responses:
        '200':
          description: User details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
        '400':
          description: Invalid user ID format
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: User not found

  /api/auth/sessions:
    post:
      summary: User login
      description: |
        Authenticates with a password, email OTP, or phone OTP and returns an
        access token.
        Existing password clients may omit `method`; it defaults to `password`.

        With `method: otp`, InsForge verifies and consumes the 6-digit sign-in
        code, keyed on exactly one of `email` or `phone`. If the identifier is
        new and public signups are enabled, it then creates a verified
        passwordless user (phone sign-ups have a null email). If public signups
        are disabled, a valid code for an unknown identifier is consumed and the
        request returns 403.

        For web clients, this endpoint sets an httpOnly refresh token cookie.
        For mobile/desktop/server clients, it returns refreshToken in the body.
      tags:
        - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: |
            Client type determines how refresh tokens are returned:
            - web: Refresh token stored in httpOnly cookie, csrfToken returned in response
            - mobile/desktop/server: refreshToken returned directly in response body
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - title: Password login
                  type: object
                  required:
                    - email
                    - password
                  properties:
                    method:
                      type: string
                      enum: [password]
                      default: password
                      description: May be omitted for backward compatibility
                    email:
                      type: string
                      format: email
                    password:
                      type: string
                - title: Email OTP login
                  type: object
                  required:
                    - method
                    - email
                    - otp
                  properties:
                    method:
                      type: string
                      enum: [otp]
                    email:
                      type: string
                      format: email
                      example: user@example.com
                    otp:
                      type: string
                      pattern: '^\d{6}$'
                      description: 6-digit sign-in code
                      example: "123456"
                    name:
                      type: string
                      minLength: 1
                      maxLength: 100
                      description: Profile name used only when a new user is created
                      example: Ada Lovelace
                - title: Phone OTP login
                  type: object
                  required:
                    - method
                    - phone
                    - otp
                  properties:
                    method:
                      type: string
                      enum: [otp]
                    phone:
                      type: string
                      pattern: '^\+[1-9]\d{6,14}$'
                      description: E.164 phone number. Provide phone or email, never both
                      example: "+15551234567"
                    otp:
                      type: string
                      pattern: '^\d{6}$'
                      description: 6-digit sign-in code
                      example: "123456"
                    name:
                      type: string
                      minLength: 1
                      maxLength: 100
                      description: Profile name used only when a new user is created
                      example: Ada Lovelace
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserResponse'
                  accessToken:
                    type: string
                  csrfToken:
                    type: string
                    nullable: true
                    description: CSRF token for use with refresh endpoint (web clients only)
                  refreshToken:
                    type: string
                    nullable: true
                    description: Refresh token for mobile/desktop/server clients (null for web clients)
        '400':
          description: Invalid input or invalid, expired, consumed, or attempt-limited sign-in code
        '401':
          description: Invalid password credentials
        '403':
          description: Email verification required, or public signups are disabled for a verified unknown email
        '429':
          description: Too many OTP verification attempts

  /api/auth/refresh:
    post:
      summary: Refresh access token
      description: |
        Refresh access token using refresh token.
        - Web clients: Use httpOnly refresh token cookie with X-CSRF-Token header
        - Mobile/Desktop/Server clients: Send refreshToken in request body
      tags:
        - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: |
            Client type determines how refresh tokens are handled:
            - web: Refresh token from httpOnly cookie, requires X-CSRF-Token header
            - mobile/desktop/server: refreshToken provided in request body, new refreshToken returned in response
        - name: X-CSRF-Token
          in: header
          schema:
            type: string
          description: CSRF token received from login/register response (required for web clients only)
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                refreshToken:
                  type: string
                  description: Refresh token (required for mobile/desktop/server clients only)
      responses:
        '200':
          description: Token refreshed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserResponse'
                  accessToken:
                    type: string
                  csrfToken:
                    type: string
                    nullable: true
                    description: New CSRF token for subsequent refresh requests (web clients only)
                  refreshToken:
                    type: string
                    nullable: true
                    description: New refresh token for mobile/desktop/server clients (must be persisted for next refresh)
        '401':
          description: No refresh token provided or user not found
        '403':
          description: Invalid CSRF token

  /api/auth/logout:
    post:
      summary: Logout user
      description: |
        Logout the current client session.

        Web clients use the httpOnly refresh token cookie. If a valid web refresh cookie is present,
        the request must include the `X-CSRF-Token` header returned from login, registration, or refresh.
        Missing, expired, invalid, or wrong-session-type refresh cookies are cleared idempotently and still
        return success. Mobile, desktop, and server clients do not have server-side logout state and should
        discard their stored refresh token after this request.
      tags:
        - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: Client type. Web clients clear the httpOnly refresh cookie; other clients should discard their local refresh token.
        - name: X-CSRF-Token
          in: header
          schema:
            type: string
          description: CSRF token required for web clients when a valid refresh cookie is present
      responses:
        '200':
          description: Logged out successfully. Also returned when the refresh cookie is missing, expired, invalid, or the wrong session type.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '403':
          description: Invalid or missing CSRF token for a valid web refresh cookie

  /api/auth/sessions/current:
    get:
      summary: Get current session
      description: |
        Returns the currently authenticated user's basic info from the access token.
        Project admin tokens return a projectAdmin session object instead of an auth.users row.

        This endpoint does not refresh expired access tokens by itself.
        For browser apps using the TypeScript SDK, call `auth.getCurrentUser()`
        during startup. The SDK will use the httpOnly refresh cookie automatically
        when it can refresh the session. Server, mobile, and other non-browser
        clients should call `/api/auth/refresh` explicitly.
      tags:
        - Client
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current session info
          content:
            application/json:
              schema:
                oneOf:
                  - type: object
                    required:
                      - user
                    additionalProperties: false
                    properties:
                      user:
                        $ref: '#/components/schemas/UserResponse'
                  - type: object
                    required:
                      - projectAdmin
                    additionalProperties: false
                    properties:
                      projectAdmin:
                        $ref: '#/components/schemas/ProjectAdminResponse'
        '401':
          description: Unauthorized

  /api/auth/admin/sessions:
    post:
      summary: Admin login
      description: Authenticates admin user for dashboard access
      tags:
        - Admin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - username
                - password
              properties:
                username:
                  type: string
                  example: admin
                password:
                  type: string
      responses:
        '200':
          description: Admin login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  projectAdmin:
                    $ref: '#/components/schemas/ProjectAdminResponse'
                  accessToken:
                    type: string
                  csrfToken:
                    type: string
                    description: CSRF token for `/api/auth/admin/refresh`
        '401':
          description: Invalid credentials
        '403':
          description: User is not an admin

  /api/auth/admin/sessions/exchange:
    post:
      summary: Exchange cloud provider authorization code for admin session
      description: Verifies an authorization code/JWT from from Insforge Cloud platform and issues an internal admin session token with project_admin role
      tags:
        - Admin
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
                  description: Authorization code or JWT from the Insforge
                  example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
      responses:
        '200':
          description: Cloud authorization verified, admin session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  projectAdmin:
                    $ref: '#/components/schemas/ProjectAdminResponse'
                  accessToken:
                    type: string
                    description: Internal JWT for admin authentication
                  csrfToken:
                    type: string
                    description: CSRF token for `/api/auth/admin/refresh`
        '400':
          description: Invalid authorization code or JWT verification failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/auth/admin/refresh:
    post:
      summary: Refresh admin dashboard access token
      description: Uses the dashboard-only httpOnly `insforge_admin_refresh_token` cookie with an `X-CSRF-Token` header.
      tags:
        - Admin
      parameters:
        - name: X-CSRF-Token
          in: header
          required: true
          schema:
            type: string
          description: CSRF token returned from admin login or admin refresh
      responses:
        '200':
          description: Admin token refreshed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  projectAdmin:
                    $ref: '#/components/schemas/ProjectAdminResponse'
                  accessToken:
                    type: string
                  csrfToken:
                    type: string
                    description: CSRF token for the next admin refresh request
        '401':
          description: No admin refresh token provided or project admin not found
        '403':
          description: Invalid CSRF token

  /api/auth/admin/logout:
    post:
      summary: Logout admin dashboard session
      description: |
        Clears the dashboard-only `insforge_admin_refresh_token` cookie.

        If a valid admin refresh cookie is present, the request must include the `X-CSRF-Token`
        header returned from admin login, admin session exchange, or admin refresh. Missing, expired,
        invalid, or wrong-session-type admin refresh cookies are cleared idempotently and still return
        success.
      tags:
        - Admin
      parameters:
        - name: X-CSRF-Token
          in: header
          schema:
            type: string
          description: CSRF token required when a valid admin refresh cookie is present
      responses:
        '200':
          description: Logged out successfully. Also returned when the admin refresh cookie is missing, expired, invalid, or the wrong session type.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '403':
          description: Invalid or missing CSRF token for a valid admin refresh cookie

  /api/auth/tokens/anon:
    post:
      summary: Get anon key (deprecated)
      deprecated: true
      description: >-
        Deprecated - use GET /api/metadata/anon-key instead. Returns the
        project's opaque anon key (`anon_...`) under the legacy `accessToken`
        field for backward compatibility. The anon key is a non-secret client
        identifier that maps requests to the `anon` role; it replaces the
        former non-expiring anonymous JWT and can be rotated via
        POST /api/secrets/anon-key/rotate (admin only).
      tags:
        - Admin
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Anon key retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  accessToken:
                    type: string
                    description: Opaque anon key (legacy field name kept for compatibility)
                    example: "anon_1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcd"
                  message:
                    type: string
                    description: Success message
                    example: "Anon key retrieved successfully (deprecated route, use GET /api/metadata/anon-key)"
        '401':
          description: Unauthorized - requires authentication
        '403':
          description: Forbidden - admin access required
        '404':
          description: Anon key not initialized

  /api/auth/email/send-otp:
    post:
      summary: Send an email sign-in OTP
      description: |
        Sends a 6-digit code that can be exchanged at POST /api/auth/sessions
        with `method: otp`.
        The response is intentionally generic and does not reveal whether a user
        already exists. The code expires after 5 minutes.
      tags:
        - Client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
                  example: user@example.com
      responses:
        '202':
          description: The sign-in code was accepted for delivery
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: If sign-in is available for this email, we have sent a verification code.
        '400':
          description: Invalid request
        '429':
          description: Too many sign-in code requests

  /api/auth/phone/send-otp:
    post:
      summary: Send an SMS sign-in OTP
      description: |
        Sends a 6-digit code over SMS that can be exchanged at
        POST /api/auth/sessions with `method: otp` and `phone`.
        The response is intentionally generic and does not reveal whether a user
        already exists. The code expires after 5 minutes. Requires a configured
        SMS provider; otherwise the request fails with
        SMS_PROVIDER_NOT_CONFIGURED.
      tags:
        - Client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
              properties:
                phone:
                  type: string
                  pattern: '^\+[1-9]\d{6,14}$'
                  description: E.164 phone number
                  example: "+15551234567"
      responses:
        '202':
          description: The sign-in code was accepted for delivery
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  message:
                    type: string
                    example: If sign-in is available for this phone number, we have sent a verification code.
        '400':
          description: Invalid request, or no SMS provider is configured
        '429':
          description: Too many sign-in code requests

  /api/auth/email/send-verification:
    post:
      summary: Send email verification (code or link based on config)
      description: Send email verification using the method configured in auth settings (verifyEmailMethod). When method is 'code', sends a 6-digit numeric code. When method is 'link', sends a browser verification link that goes through an InsForge backend endpoint first. Prevents user enumeration by returning success even if email doesn't exist.
      tags:
        - Client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
                  example: user@example.com
                redirectTo:
                  type: string
                  format: uri
                  description: >-
                    Used for link-based email verification. The email link always
                    opens an InsForge backend endpoint first; after the token is
                    verified, InsForge redirects the browser to this URL. This URL
                    must be included in allowedRedirectUrls. Recommended: use your
                    app's sign-in page.
      responses:
        '202':
          description: Verification email sent (if email exists). Message varies based on configured method.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                    example: "If your email is registered, we have sent you a verification code/link. Please check your inbox."
        '400':
          description: Invalid request

  /api/auth/email/verify-link:
    get:
      summary: Verify email from browser link click
      description: |
        Browser-oriented link verification flow.

        This endpoint is intended for users clicking verification links in email.
        It validates the token on the backend and redirects the browser to the
        stored, validated `redirectTo` URL with the verification result.

        Redirect query params:
        - Success: `insforge_status=success&insforge_type=verify_email`
        - Error: `insforge_status=error&insforge_type=verify_email&insforge_error=...`
        - `insforge_status`: `success` or `error`
        - `insforge_type`: always `verify_email`
        - `insforge_error`: present only on error, human-readable message

        Recommended handling: use your sign-in page as `redirectTo`. When the
        redirect arrives with `insforge_status=success`, show a confirmation message
        and ask the user to sign in with their email and password.
      tags:
        - Client
      parameters:
        - name: token
          in: query
          required: true
          schema:
            type: string
          description: 64-character email verification token from the email link
      responses:
        '302':
          description: Browser redirected to the stored redirect URL
        '400':
          description: Invalid verification token or redirect target

  /api/auth/email/verify:
    post:
      summary: Verify email with code
      description: |
        Verify email address with a 6-digit code.

        Successfully verified users will receive a session token.

        Browser email clicks should use `GET /api/auth/email/verify-link`.
        `POST /api/auth/email/verify` is the JSON API for 6-digit code submission.
      tags:
        - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: |
            Client type determines how refresh tokens are returned:
            - web: Refresh token stored in httpOnly cookie, csrfToken returned in response
            - mobile/desktop/server: refreshToken returned directly in response body
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - otp
              properties:
                email:
                  type: string
                  format: email
                  description: User email address
                  example: user@example.com
                otp:
                  type: string
                  pattern: '^\d{6}$'
                  description: 6-digit verification code
                  example: "123456"
      responses:
        '200':
          description: Email verified successfully, session created
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserResponse'
                  accessToken:
                    type: string
                    description: JWT authentication token
                  csrfToken:
                    type: string
                    nullable: true
                    description: CSRF token for use with refresh endpoint (web clients only)
                  refreshToken:
                    type: string
                    nullable: true
                    description: Refresh token for mobile/desktop/server clients (null for web clients)
        '400':
          description: Invalid verification code or token
        '401':
          description: Verification code/token expired or invalid

  /api/auth/email/send-reset-password:
    post:
      summary: Send password reset (code or link based on config)
      description: Send password reset email using the method configured in auth settings (resetPasswordMethod). When method is 'code', sends a 6-digit numeric code for two-step flow. When method is 'link', sends a browser reset link that goes through an InsForge backend endpoint first. Prevents user enumeration by returning success even if email doesn't exist.
      tags:
        - Client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
              properties:
                email:
                  type: string
                  format: email
                  example: user@example.com
                redirectTo:
                  type: string
                  format: uri
                  description: >-
                    Used for link-based password reset. The email link always
                    opens an InsForge backend endpoint first; InsForge then
                    redirects the browser to this URL with the reset token in
                    the query string. This URL must be included in
                    allowedRedirectUrls. Recommended: use your app's dedicated
                    reset-password page.
      responses:
        '202':
          description: Password reset email sent (if email exists). Message varies based on configured method.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
                    example: "If your email is registered, we have sent you a password reset code/link. Please check your inbox."
        '400':
          description: Invalid request

  /api/auth/email/exchange-reset-password-token:
    post:
      summary: Exchange reset password code for reset token
      description: |
        Step 1 of two-step password reset flow (only used when resetPasswordMethod is 'code'):
        1. Verify the 6-digit code sent to user's email
        2. Return a reset token that can be used to actually reset the password

        This endpoint is not used when resetPasswordMethod is 'link', because the browser reset-link flow uses the emailed link token directly.
      tags:
        - Client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - email
                - code
              properties:
                email:
                  type: string
                  format: email
                  example: user@example.com
                code:
                  type: string
                  pattern: '^\d{6}$'
                  description: 6-digit numeric code from email
                  example: "123456"
      responses:
        '200':
          description: Code verified successfully, reset token returned
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                    description: Reset token to be used in reset-password endpoint
                  expiresAt:
                    type: string
                    format: date-time
                    description: Token expiration timestamp
        '400':
          description: Invalid request
        '401':
          description: Invalid or expired code

  /api/auth/email/reset-password-link:
    get:
      summary: Open password reset flow from browser link click
      description: |
        Browser-oriented password reset link flow.

        This endpoint is intended for users clicking password reset links in email.
        It validates the token on the backend and redirects the browser to the
        stored, validated `redirectTo` URL with the reset token in the query string.

        Redirect query params:
        - Ready: `token=...&insforge_status=ready&insforge_type=reset_password`
        - Error: `insforge_status=error&insforge_type=reset_password&insforge_error=...`
        - `token`: present only when `insforge_status=ready`
        - `insforge_status`: `ready` or `error`
        - `insforge_type`: always `reset_password`
        - `insforge_error`: present only on error, human-readable message

        Your app should render the reset-password form only when `insforge_status=ready`
        and `token` is present.
      tags:
        - Client
      parameters:
        - name: token
          in: query
          required: true
          schema:
            type: string
          description: 64-character password reset token from the email link
      responses:
        '302':
          description: Browser redirected to the stored redirect URL
        '400':
          description: Invalid reset token or redirect target

  /api/auth/email/reset-password:
    post:
      summary: Reset password with token
      description: |
        Reset user password with a token. The token can be:
        - Magic link token (64-character hex token from send-reset-password when method is 'link')
        - Reset token (from exchange-reset-password-token after code verification when method is 'code')

        Both token types use RESET_PASSWORD purpose and are verified the same way.

        Flow summary:
        - Code method: send-reset-password → exchange-reset-password-token → reset-password (with resetToken)
        - Link method: send-reset-password → GET /api/auth/email/reset-password-link → reset-password
      tags:
        - Client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - newPassword
                - otp
              properties:
                newPassword:
                  type: string
                  description: New password meeting configured requirements
                  example: newSecurePassword123
                otp:
                  type: string
                  description: Reset token (either from magic link or from exchange-reset-password-token endpoint)
                  example: "a1b2c3d4..."
      responses:
        '200':
          description: Password reset successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: "Password reset successfully"
        '400':
          description: Invalid request or password requirements not met
        '401':
          description: Verification code/token expired or invalid

  /api/auth/oauth/configs:
    get:
      summary: List all OAuth configurations
      description: Get all configured OAuth providers (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of OAuth configurations
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/OAuthConfig'
                  count:
                    type: integer
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

    post:
      summary: Create OAuth configuration
      description: Create a new OAuth provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - provider
              properties:
                provider:
                  type: string
                  enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
                clientId:
                  type: string
                nativeClientIds:
                  type: array
                  maxItems: 20
                  description: Server-trusted native app client IDs used as ID-token audiences
                  items:
                    type: string
                    minLength: 1
                    maxLength: 255
                clientSecret:
                  type: string
                redirectUri:
                  type: string
                scopes:
                  type: array
                  items:
                    type: string
                useSharedKey:
                  type: boolean
      responses:
        '200':
          description: OAuth configuration created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthConfig'
        '400':
          description: Invalid request
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

  /api/auth/oauth/{provider}/config:
    get:
      summary: Get OAuth configuration for specific provider
      description: Get OAuth configuration including client secret (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
      responses:
        '200':
          description: OAuth configuration
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/OAuthConfig'
                  - type: object
                    properties:
                      clientSecret:
                        type: string
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: Configuration not found

    put:
      summary: Update OAuth configuration
      description: Update OAuth provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                clientId:
                  type: string
                nativeClientIds:
                  type: array
                  maxItems: 20
                  description: Server-trusted native app client IDs used as ID-token audiences
                  items:
                    type: string
                    minLength: 1
                    maxLength: 255
                clientSecret:
                  type: string
                redirectUri:
                  type: string
                scopes:
                  type: array
                  items:
                    type: string
                useSharedKey:
                  type: boolean
      responses:
        '200':
          description: Configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OAuthConfig'
        '400':
          description: Invalid request
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: Configuration not found

    delete:
      summary: Delete OAuth configuration
      description: Delete OAuth provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
      responses:
        '200':
          description: Configuration deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: Configuration not found

  /api/auth/oauth/{provider}:
    get:
      summary: Initiate OAuth flow (PKCE)
      description: |
        Generate OAuth authorization URL for any supported provider using PKCE flow.

        For mobile/desktop/server clients using PKCE:
        1. Generate code_verifier (random string, 43-128 chars)
        2. Generate code_challenge = Base64URL(SHA256(code_verifier))
        3. Call this endpoint with code_challenge
        4. After OAuth callback, use /api/auth/oauth/exchange with code_verifier
      tags:
        - Client
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
        - name: redirect_uri
          in: query
          required: true
          schema:
            type: string
            format: uri
          description: URL to redirect after authentication (receives insforge_code parameter)
        - name: code_challenge
          in: query
          required: false
          schema:
            type: string
          description: |
            PKCE code challenge for mobile/desktop/server clients.
            Generate using: Base64URL(SHA256(code_verifier))
            Required for secure token exchange in native apps or trusted server-side clients.
      responses:
        '200':
          description: OAuth authorization URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  authUrl:
                    type: string
                    format: uri
                    description: URL to redirect user for OAuth provider login
        '400':
          description: Invalid request or provider not supported
        '500':
          description: OAuth not configured

  /api/auth/oauth/exchange:
    post:
      summary: Exchange OAuth code for tokens (PKCE)
      description: |
        Exchange the insforge_code (received from OAuth callback) for access and refresh tokens.

        This endpoint is used for PKCE flow in mobile/desktop/server clients:
        1. After OAuth callback, your redirect_uri receives `insforge_code` parameter
        2. Call this endpoint with the code and your original code_verifier
        3. Receive access token and refresh token in response

        The code_verifier must match the code_challenge sent during OAuth initiation.
      tags:
        - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: |
            Client type determines how refresh tokens are returned:
            - web: Refresh token stored in httpOnly cookie, csrfToken returned in response
            - mobile/desktop/server: refreshToken returned directly in response body
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
                - code_verifier
              properties:
                code:
                  type: string
                  description: The insforge_code received from OAuth callback redirect
                  example: "abc123..."
                code_verifier:
                  type: string
                  description: |
                    The original code_verifier used to generate code_challenge.
                    Must be 43-128 characters, using [A-Za-z0-9-._~]
                  example: "dBjftJeZ4CVP-mB92K27uhbUJU1p1r_wW1gFWFOEjXk"
      responses:
        '200':
          description: Tokens exchanged successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserResponse'
                  accessToken:
                    type: string
                    description: JWT access token for API authentication
                  refreshToken:
                    type: string
                    description: Refresh token for obtaining new access tokens
        '400':
          description: Invalid request - missing code or code_verifier
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Invalid or expired code, or code_verifier mismatch
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'

  /api/auth/oauth/shared/callback/{state}:
    get:
      summary: Shared OAuth callback handler
      description: Handles OAuth callbacks from InsForge Cloud shared OAuth
      tags:
        - Client
      parameters:
        - name: state
          in: path
          required: true
          schema:
            type: string
          description: JWT state parameter
        - name: success
          in: query
          schema:
            type: string
          description: Success flag
        - name: error
          in: query
          schema:
            type: string
          description: Error message
        - name: payload
          in: query
          schema:
            type: string
          description: Base64 encoded user payload
      responses:
        '302':
          description: Redirect to application with access token or error
          headers:
            Location:
              schema:
                type: string
                format: uri

  /api/auth/oauth/{provider}/callback:
    get:
      summary: Provider-specific OAuth callback (GET)
      description: |
        OAuth callback endpoint for provider-specific flows (most providers use GET).

        Response varies based on the original OAuth initiation:
        - With code_challenge (PKCE): Redirects with `insforge_code` for exchange endpoint
        - Without code_challenge (web): Redirects with `access_token` and sets httpOnly cookie
      tags:
        - Client
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
        - name: code
          in: query
          schema:
            type: string
          description: Authorization code from OAuth provider
        - name: state
          in: query
          required: true
          schema:
            type: string
          description: JWT state with redirect URI and optional code_challenge
        - name: token
          in: query
          schema:
            type: string
          description: Direct ID token (for some providers)
      responses:
        '302':
          description: |
            Redirect to application.
            - PKCE flow: redirect_uri?insforge_code={code}&user_id={id}&email={email}&name={name}
            - Web flow: redirect_uri?access_token={token}&user_id={id}&email={email}&name={name}&csrf_token={csrf}
          headers:
            Location:
              schema:
                type: string
                format: uri
                description: Redirect URL with authentication parameters

    post:
      summary: Provider-specific OAuth callback (POST)
      description: |
        OAuth callback endpoint for providers that use POST (e.g., Apple with form_post response mode).

        Response varies based on the original OAuth initiation:
        - With code_challenge (PKCE): Redirects with `insforge_code` for exchange endpoint
        - Without code_challenge (web): Redirects with `access_token` and sets httpOnly cookie
      tags:
        - Client
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
            enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - state
              properties:
                code:
                  type: string
                  description: Authorization code from OAuth provider
                state:
                  type: string
                  description: JWT state with redirect URI and optional code_challenge
                id_token:
                  type: string
                  description: ID token (for Apple Sign In)
      responses:
        '302':
          description: |
            Redirect to application.
            - PKCE flow: redirect_uri?insforge_code={code}&user_id={id}&email={email}&name={name}
            - Web flow: redirect_uri?access_token={token}&user_id={id}&email={email}&name={name}&csrf_token={csrf}
          headers:
            Location:
              schema:
                type: string
                format: uri
                description: Redirect URL with authentication parameters

  /api/auth/oauth/custom/configs:
    get:
      summary: List all custom OAuth configurations
      description: Get all configured custom OAuth providers (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of custom OAuth configurations
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CustomOAuthConfig'
                  count:
                    type: integer
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

    post:
      summary: Create custom OAuth configuration
      description: Create a new custom OAuth provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - key
                - name
                - discoveryEndpoint
                - clientId
                - clientSecret
              properties:
                key:
                  type: string
                name:
                  type: string
                discoveryEndpoint:
                  type: string
                  format: uri
                clientId:
                  type: string
                clientSecret:
                  type: string
      responses:
        '200':
          description: Custom OAuth configuration created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomOAuthConfig'
        '400':
          description: Invalid request
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '409':
          description: Configuration already exists

  /api/auth/oauth/custom/{key}/config:
    get:
      summary: Get custom OAuth configuration
      description: Get a custom OAuth configuration including client secret (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Custom OAuth configuration
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/CustomOAuthConfig'
                  - type: object
                    properties:
                      clientSecret:
                        type: string
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: Configuration not found

    put:
      summary: Update custom OAuth configuration
      description: Update a custom OAuth provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                discoveryEndpoint:
                  type: string
                  format: uri
                clientId:
                  type: string
                clientSecret:
                  type: string
      responses:
        '200':
          description: Custom OAuth configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomOAuthConfig'
        '400':
          description: Invalid request
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: Configuration not found

    delete:
      summary: Delete custom OAuth configuration
      description: Delete a custom OAuth provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Configuration deleted
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  message:
                    type: string
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only
        '404':
          description: Configuration not found

  /api/auth/oauth/custom/{key}:
    get:
      summary: Initiate custom OAuth flow (PKCE)
      description: Generate OAuth authorization URL for a configured custom OAuth provider using PKCE flow
      tags:
        - Client
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: redirect_uri
          in: query
          required: true
          schema:
            type: string
            format: uri
          description: URL to redirect after authentication (receives insforge_code parameter)
        - name: code_challenge
          in: query
          required: false
          schema:
            type: string
          description: PKCE code challenge for mobile/desktop/server clients
      responses:
        '200':
          description: OAuth authorization URL
          content:
            application/json:
              schema:
                type: object
                properties:
                  authUrl:
                    type: string
                    format: uri
        '400':
          description: Invalid request
        '404':
          description: Custom OAuth provider not found

  /api/auth/oauth/custom/{key}/callback:
    get:
      summary: Custom OAuth callback (GET)
      description: OAuth callback endpoint for custom OAuth providers
      tags:
        - Client
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: code
          in: query
          required: true
          schema:
            type: string
          description: Authorization code from the custom OAuth provider
        - name: state
          in: query
          required: true
          schema:
            type: string
          description: Signed OAuth state payload
      responses:
        '302':
          description: Redirect to application after custom OAuth completion
          headers:
            Location:
              schema:
                type: string
                format: uri
                description: Redirect URL with authentication parameters

  /api/auth/id-token:
    post:
      summary: Sign in with a native provider ID token
      description: |
        Authenticate a user using an ID token obtained from Google or Apple's native SDK.
        Apple requests require the original raw nonce whose lowercase SHA-256 hex digest was
        attached to the native authorization request. Apple token audiences are validated only
        against the project's configured clientId and nativeClientIds; callers cannot select a
        trusted audience.
      tags:
        - Client
      parameters:
        - name: client_type
          in: query
          schema:
            type: string
            enum: [web, mobile, desktop, server]
            default: web
          description: |
            Client type determines how refresh tokens are returned:
            - web: Refresh token stored in httpOnly cookie, csrfToken returned in response
            - mobile/desktop/server: refreshToken returned directly in response body
      requestBody:
        required: true
        content:
          application/json:
            schema:
              oneOf:
                - $ref: '#/components/schemas/GoogleIdTokenSignInRequest'
                - $ref: '#/components/schemas/AppleIdTokenSignInRequest'
              discriminator:
                propertyName: provider
                mapping:
                  google: '#/components/schemas/GoogleIdTokenSignInRequest'
                  apple: '#/components/schemas/AppleIdTokenSignInRequest'
      responses:
        '200':
          description: Sign-in successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  user:
                    $ref: '#/components/schemas/UserResponse'
                  accessToken:
                    type: string
                  csrfToken:
                    type: string
                    nullable: true
                    description: CSRF token for use with refresh endpoint (web clients only)
                  refreshToken:
                    type: string
                    nullable: true
                    description: Refresh token for mobile/desktop/server clients (null for web clients)
        '400':
          description: Invalid request - unsupported provider or missing token
        '401':
          description: Invalid or expired ID token

  /api/auth/admin/sessions/current:
    get:
      summary: Get current admin session
      description: Returns the currently authenticated admin's basic info from the access token.
      tags:
        - Admin
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Current admin session info
          content:
            application/json:
              schema:
                type: object
                properties:
                  admin:
                    type: object
                    properties:
                      sub:
                        type: string
                        description: Admin subject identifier
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin access required

  /api/auth/smtp-config:
    get:
      summary: Get SMTP configuration
      description: Get current SMTP email configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      x-internal: true
      responses:
        '200':
          description: SMTP configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SmtpConfig'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

    put:
      summary: Update SMTP configuration
      description: |
        Create or update SMTP email configuration (admin only).
        When `enabled` is true, the fields `host`, `username`, `senderEmail`, and `senderName` are required by the backend validation.
        When `enabled` is false, these fields may be empty.
        SMTP cannot be disabled on a self-hosted instance while email verification is required, unless a managed email provider is available.
      tags:
        - Admin
      security:
        - bearerAuth: []
      x-internal: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - enabled
                - port
              properties:
                enabled:
                  type: boolean
                  description: When true, host/username/senderEmail/senderName become required
                host:
                  type: string
                  description: Required when enabled is true
                port:
                  type: integer
                  enum: [25, 465, 587, 2525]
                username:
                  type: string
                  description: Required when enabled is true
                password:
                  type: string
                  description: SMTP password (optional on update if unchanged)
                senderEmail:
                  type: string
                  format: email
                  description: Required when enabled is true
                senderName:
                  type: string
                  description: Required when enabled is true
                minIntervalSeconds:
                  type: integer
                  minimum: 0
                  default: 60
                  description: Minimum interval between emails in seconds
      responses:
        '200':
          description: SMTP configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SmtpConfig'
        '400':
          description: Invalid request, or SMTP is the required email verification provider
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

  /api/auth/sms-config:
    get:
      summary: Get SMS configuration
      description: Get current SMS provider configuration (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      x-internal: true
      responses:
        '200':
          description: SMS configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SmsConfig'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

    put:
      summary: Update SMS configuration
      description: |
        Create or update SMS provider configuration (admin only).
        When `enabled` is true with the `twilio` provider, `accountSid` and a
        `fromNumber` or `messagingServiceSid` are required, and the credentials
        are verified against the Twilio API before persisting.
        The `console` provider logs messages instead of sending them and is
        rejected in production.
      tags:
        - Admin
      security:
        - bearerAuth: []
      x-internal: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - enabled
              properties:
                enabled:
                  type: boolean
                  description: When true, provider credentials become required
                provider:
                  type: string
                  enum: [twilio, console]
                  default: twilio
                accountSid:
                  type: string
                  description: Required when enabled is true with the twilio provider
                authToken:
                  type: string
                  description: Twilio auth token (optional on update if unchanged)
                fromNumber:
                  type: string
                  description: E.164 sender number. Either this or messagingServiceSid is required when enabling twilio
                messagingServiceSid:
                  type: string
                  description: Used instead of fromNumber when set
                minIntervalSeconds:
                  type: integer
                  minimum: 0
                  default: 60
                  description: Minimum interval between messages to the same number in seconds
                otpMessageTemplate:
                  type: string
                  maxLength: 320
                  description: Sign-in message body; must contain the {{ code }} placeholder when enabling
      responses:
        '200':
          description: SMS configuration updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SmsConfig'
        '400':
          description: Invalid request, or the Twilio credential check failed
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

  /api/auth/email-templates:
    get:
      summary: Get all email templates
      description: Returns all configurable email templates (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      x-internal: true
      responses:
        '200':
          description: List of email templates
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/EmailTemplate'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

  /api/auth/email-templates/{type}:
    put:
      summary: Update email template
      description: Update a specific email template by type (admin only)
      tags:
        - Admin
      security:
        - bearerAuth: []
      x-internal: true
      parameters:
        - name: type
          in: path
          required: true
          schema:
            type: string
            enum: [email-verification-code, email-verification-link, request-otp, reset-password-code, reset-password-link]
          description: Template type to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - subject
                - bodyHtml
              properties:
                subject:
                  type: string
                  description: Email subject line
                bodyHtml:
                  type: string
                  description: HTML body of the email template
      responses:
        '200':
          description: Template updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EmailTemplate'
        '400':
          description: Invalid template type or request body
        '401':
          description: Unauthorized
        '403':
          description: Forbidden - Admin only

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    apiKey:
      type: apiKey
      in: header
      name: x-api-key

  schemas:
    GoogleIdTokenSignInRequest:
      type: object
      required: [provider, token]
      properties:
        provider:
          type: string
          enum: [google]
        token:
          type: string
          minLength: 1
          description: Raw Google ID token

    AppleIdTokenSignInRequest:
      type: object
      additionalProperties: false
      required: [provider, token, nonce]
      properties:
        provider:
          type: string
          enum: [apple]
        token:
          type: string
          minLength: 1
          description: Raw identityToken from ASAuthorizationAppleIDCredential
        nonce:
          type: string
          minLength: 1
          maxLength: 255
          description: Original raw nonce whose lowercase SHA-256 hex digest was attached to ASAuthorizationAppleIDRequest
        name:
          type: string
          minLength: 1
          maxLength: 100
          description: Optional display name returned on first Apple authorization

    ProjectAdminResponse:
      type: object
      required:
        - subject
        - username
      properties:
        subject:
          type: string
          example: local:admin
        username:
          type: string
          example: admin

    UserResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
          nullable: true
          description: Null for accounts created by phone sign-in
        phone:
          type: string
          nullable: true
          description: E.164 phone number, null for accounts without one
        phoneVerified:
          type: boolean
        profile:
          type: object
          nullable: true
          additionalProperties: true
          description: User profile data (name, avatar_url, and custom fields)
          properties:
            name:
              type: string
            avatar_url:
              type: string
              format: uri
        metadata:
          type: object
          nullable: true
          additionalProperties: true
          description: System metadata (device ID, login IP, etc.)
        emailVerified:
          type: boolean
        providers:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ProfileResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: User ID
        profile:
          type: object
          nullable: true
          additionalProperties: true
          description: User profile data (can contain custom fields)
          properties:
            name:
              type: string
            avatar_url:
              type: string
              format: uri

    OAuthConfig:
      type: object
      properties:
        id:
          type: string
          format: uuid
        provider:
          type: string
          enum: [google, github, discord, linkedin, facebook, instagram, tiktok, apple, x, spotify, microsoft]
        clientId:
          type: string
          nullable: true
        nativeClientIds:
          type: array
          maxItems: 20
          description: Server-trusted native app client IDs used as ID-token audiences
          items:
            type: string
            minLength: 1
            maxLength: 255
        redirectUri:
          type: string
          nullable: true
        scopes:
          type: array
          items:
            type: string
          nullable: true
        useSharedKey:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CustomOAuthConfig:
      type: object
      properties:
        id:
          type: string
          format: uuid
        key:
          type: string
        name:
          type: string
        discoveryEndpoint:
          type: string
          format: uri
        clientId:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AuthRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        passwordHash:
          type: string
          description: SHA256 hash of password
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ProfileRecord:
      type: object
      properties:
        id:
          type: string
          format: uuid
        authId:
          type: string
          format: uuid
          description: Foreign key to auth table
        name:
          type: string
        avatar_url:
          type: string
          nullable: true
        bio:
          type: string
          nullable: true
        metadata:
          type: object
          description: JSONB field for flexible data
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    ErrorResponse:
      type: object
      required:
        - error
        - message
        - statusCode
      properties:
        error:
          type: string
          description: Error code for programmatic handling
          example: "VALIDATION_ERROR"
        message:
          type: string
          description: Human-readable error message
          example: "Email is already in use"
        statusCode:
          type: integer
          description: HTTP status code
          example: 400
        nextActions:
          type: string
          description: Suggested action to resolve the error
          example: "Please use a different email address"

    SmtpConfig:
      type: object
      properties:
        id:
          type: string
          format: uuid
        enabled:
          type: boolean
        host:
          type: string
        port:
          type: integer
        username:
          type: string
        hasPassword:
          type: boolean
          description: Whether a password is set (actual password is never exposed)
        senderEmail:
          type: string
          format: email
        senderName:
          type: string
        minIntervalSeconds:
          type: integer
          minimum: 0
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    SmsConfig:
      type: object
      properties:
        id:
          type: string
          format: uuid
        enabled:
          type: boolean
        provider:
          type: string
          enum: [twilio, console]
        accountSid:
          type: string
        hasAuthToken:
          type: boolean
          description: Whether an auth token is set (actual token is never exposed)
        fromNumber:
          type: string
        messagingServiceSid:
          type: string
        minIntervalSeconds:
          type: integer
          minimum: 0
        otpMessageTemplate:
          type: string
          description: Sign-in message body with the {{ code }} placeholder
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    EmailTemplate:
      type: object
      properties:
        id:
          type: string
          format: uuid
        templateType:
          type: string
          enum: [email-verification-code, email-verification-link, request-otp, reset-password-code, reset-password-link]
        subject:
          type: string
        bodyHtml:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
